import React, { memo } from 'react';

import Portfolio from '../../common/portfolio'

export default memo(function PortfolioPage() {
    return (
        <div>
            <Portfolio />
        </div>
    )
})
